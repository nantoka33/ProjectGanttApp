<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectGanttApp"
        x:Name="MainWindow"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"
        Title="プロジェクト管理" Height="500" Width="1000">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding FilteredTasks}"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      IsReadOnly="False"
                      RowHeight="30">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="タスク名"
                                        Binding="{Binding Name, Mode=TwoWay}"
                                        Width="140" />
                    <DataGridTextColumn Header="期限"
                                        Binding="{Binding EndDate, Mode=TwoWay, StringFormat='M/d'}"
                                        Width="60" />

                    <DataGridTemplateColumn Header="ステータス" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Status}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.TaskStatuses, RelativeSource={RelativeSource AncestorType=Window}}"
                                          SelectedItem="{Binding Status, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>



                    <DataGridTemplateColumn Header="ガント" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Canvas Height="25">
                                    <Rectangle Width="{Binding Path=., Converter={StaticResource GanttWidthConverter}}"
                                               Height="20"
                                               Fill="{Binding Status, Converter={StaticResource StatusToBrushConverter}}"
                                               Canvas.Left="{Binding Path=., Converter={StaticResource GanttLeftConverter}}" />
                                    <TextBlock Text="{Binding Status}"
                                               Canvas.Left="3" Canvas.Top="3" FontSize="10"
                                               Foreground="Black"/>
                                </Canvas>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </ScrollViewer>
</Window>
