<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectGanttApp"
        mc:Ignorable="d"
       Title="プロジェクト管理" Height="500" Width="950">
    <DockPanel>
        <StackPanel Orientation="Horizontal" Margin="10" DockPanel.Dock="Top">
            <Button Content="タスク追加" Click="AddTask_Click" Width="100" Margin="0,0,10,0" />
            <CheckBox Content="完了以外を表示" IsChecked="{Binding ShowIncompleteOnly, Mode=TwoWay}" />
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <DataGrid Grid.Column="0" ItemsSource="{Binding FilteredTasks}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="タスク名" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="期限" Binding="{Binding EndDate, StringFormat='M/d'}" Width="60" />
                    <DataGridTextColumn Header="ステータス" Binding="{Binding Status}" Width="80" />
                </DataGrid.Columns>
            </DataGrid>
            <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto">
                <Canvas x:Name="GanttArea" Background="LightGray" Height="400" />
            </ScrollViewer>
        </Grid>
    </DockPanel>
</Window>
